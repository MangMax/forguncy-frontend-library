{
  "private": true,
  "type": "module",
  "name": "forguncy-frontend-libraries",
  "description": "活字格 ReactCellType 前端扩展包仓库（多包 monorepo）",
  "license": "MIT",
  "packageManager": "pnpm@12.3.4",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "vp run -r build",
    "pack": "vp run -r pack",
    "test": "vp run -r tests",
    "gates": "vp run -r gates",
    "verify": "vp run -r build && vp run -r gates && vp run -r pack",
    "packages": "node tooling/package-index.mjs",
    "new-package": "node tooling/new-package.mjs",
    "changelog": "node tooling/changelog.mjs"
  }
}
